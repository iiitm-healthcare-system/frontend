// The Purpose of this file is to export all the components from the Mantine Library
// so that we can use them in our app without having to specify `use client` in
// every file that uses them.
"use client";

import { Button, Title, Text, Anchor, Flex, Grid } from "@mantine/core";
import { Notifications } from "@mantine/notifications";

export { Button, Title, Text, Anchor, Flex, Grid, Notifications };
